spring:
  config:
    import:
      optional:
        dotenv:
  application:
    name: medsystem
  datasource:
    password: Pasca789@
    username: postgres
    url: jdbc:postgresql://localhost:5432/Med_ease
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
  jwt:
    secret: mysupersecurejwtsecretkeythatislongenough123


server:
  port: 8080
